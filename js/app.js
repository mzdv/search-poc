var React = require('react');
var ReactDOM = require('react-dom');

var SearchWindow = require('./components/SearchWindow.jsx');

ReactDOM.render(
  <SearchWindow />,
  document.getElementById('search')
);
